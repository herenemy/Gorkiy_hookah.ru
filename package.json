{
  "name": "gorkiy-restaurant",
  "source": "src/index.html",
  "browserslist": "> 0.5%, last 2 versions, not dead",
  "scripts": {
    "start": "parcel src/index.html --open",
    "build": "parcel build --no-source-maps --dist-dir prod"
  },
  "devDependencies": {
    "parcel": "latest"
  }
}
